# post-tracking-demo-real

Render settings:

- Build Command: `npm install && npx playwright install chromium`
- Start Command: `npm start`

Local:

```bash
npm install
npx playwright install chromium
npm start
```
